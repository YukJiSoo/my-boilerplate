import * as React from 'react';
import * as Style from './style';

const SampleComoponents = () => {
    return (
        <Style.SampleComoponents>
            This is sample component.
        </Style.SampleComoponents>
    );
};

export default SampleComoponents;
